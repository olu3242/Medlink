import{describe,expect,it,vi}from"vitest";import{ConversationService,type ConversationStore}from"./index";
describe("ConversationService",()=>{it("deduplicates provider delivery and escalates low confidence",async()=>{const transition=vi.fn().mockResolvedValue({});const store={findByProviderMessage:vi.fn().mockResolvedValue(null),session:vi.fn().mockResolvedValue({id:"s",tenantId:"t",channel:"whatsapp",channelIdentityHash:"h",status:"active",version:1}),append:vi.fn(),transition} satisfies ConversationStore;await new ConversationService(store).accept({tenantId:"t",sessionId:"s",message:{id:"m",sessionId:"s",direction:"inbound",providerMessageId:"p",kind:"text",receivedAt:new Date()},intent:{name:"unknown",confidence:.4,requiresHumanReview:false}});expect(transition).toHaveBeenCalledWith(expect.objectContaining({status:"waiting_for_human"}));});});
