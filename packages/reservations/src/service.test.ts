import{describe,expect,it}from"vitest";import{ReservationService}from"./service";
describe("reservations",()=>{it("releases lock if persistence fails",async()=>{let released=false;const s=new ReservationService({findByIdempotencyKey:async()=>null,create:async()=>{throw new Error("db");},findById:async()=>null,markExpiredAtomically:async()=>null},{lock:async()=>({id:"l"}),release:async()=>{released=true;}},{next:()=>"r"},{now:()=>new Date(0)});await expect(s.create({tenantId:"t",marId:"m",patientId:"p",pharmacyId:"x",inventoryItemId:"i",quantity:1,expiresAt:new Date(10),idempotencyKey:"k"})).rejects.toThrow("db");expect(released).toBe(true);});});
