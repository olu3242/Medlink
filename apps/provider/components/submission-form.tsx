"use client";import{useState}from"react";type Kind="prescription"|"referral";
export function SubmissionForm({kind}:{kind:Kind}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(form:FormData){setBusy(true);setMessage("");const path=kind==="prescription"?"/api/v1/prescriptions":"/api/v1/referrals";try{const r=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID()},body:JSON.stringify(Object.fromEntries(form))});if(!r.ok)throw new Error();setMessage(`${kind==="prescription"?"E-prescription":"Referral"} submitted for processing.`)}catch{setMessage("Submission failed. Verify the details and try again.")}finally{setBusy(false)}}return <form className="form card" action={submit}><div className="field"><label htmlFor="patientReference">Patient reference</label><input id="patientReference" name="patientReference" required autoComplete="off"/></div>{kind==="prescription"?<><div className="field"><label htmlFor="medicine">Medicine</label><input id="medicine" name="medicine" required/></div><div className="field"><label htmlFor="strength">Strength</label><input id="strength" name="strength" required/></div><div className="field"><label htmlFor="quantity">Quantity</label><input id="quantity" name="quantity" type="number" min="1" required/></div><div className="field full"><label htmlFor="directions">Directions</label><textarea id="directions" name="directions" required/></div></>:<><div className="field"><label htmlFor="service">Referred service</label><input id="service" name="service" required/></div><div className="field full"><label htmlFor="reason">Clinical reason</label><textarea id="reason" name="reason" required/></div></>}<div className="actions full"><button className="button" disabled={busy} type="submit">{busy?"Submitting…":"Submit securely"}</button><span aria-live="polite">{message}</span></div></form>}
