"use client";
import { useState } from "react";
import { DashboardPreview } from "./DashboardPreview";
const audiences = [{ id: "patient", label: "Patient", summary: "Find and reserve verified nearby stock without walking door to door.", rows: [{ label: "Search radius", value: "5 km" }, { label: "Matches", value: "4 verified" }] }, { id: "pharmacist", label: "Pharmacist", summary: "Review requests, clinical flags, and eligible alternatives in one queue.", rows: [{ label: "Review queue", value: "12 requests" }, { label: "High priority", value: "2" }] }, { id: "pharmacy", label: "Pharmacy", summary: "Publish current availability and manage reservation windows.", rows: [{ label: "Inventory sync", value: "Healthy" }, { label: "Pickup ready", value: "8" }] }, { id: "hospital", label: "Hospital / HMO", summary: "Follow access after prescribing while preserving patient privacy.", rows: [{ label: "Coverage", value: "Verified" }, { label: "Fulfilled", value: "94%" }] }];
export function StakeholderWorkspace() { const [active, setActive] = useState(audiences[0]!); return <section id="stakeholders" className="section section--workspace"><div className="container"><div className="workspace__tabs" role="tablist" aria-label="Stakeholder workspace">{audiences.map((audience) => <button key={audience.id} role="tab" aria-selected={active.id === audience.id} onClick={() => setActive(audience)}>{audience.label}</button>)}</div><div className="workspace__panel" role="tabpanel"><div><p className="eyebrow">One network, clear roles</p><h2>{active.label} workspace</h2><p>{active.summary}</p></div><DashboardPreview title={active.label} rows={active.rows} /></div></div></section>; }
