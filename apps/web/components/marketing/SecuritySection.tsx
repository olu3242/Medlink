import { SectionHeader } from "./SectionHeader";
const items = [{ icon: "01", title: "Isolated data tenants", body: "Organizations operate inside database-enforced data boundaries." }, { icon: "02", title: "Complete audit trail", body: "Material workflow and clinical decisions remain attributable and reviewable." }, { icon: "03", title: "Human clinical authority", body: "AI assists; licensed professionals make dispensing and substitution decisions." }];
export function SecuritySection() { return <section id="security" className="section"><div className="container"><SectionHeader eyebrow="Trust and governance" title="Built to be trusted with medicine, not just data" /><div className="security-grid">{items.map((item) => <article key={item.title}><span>{item.icon}</span><h3>{item.title}</h3><p>{item.body}</p></article>)}</div></div></section>; }
