"use client";
import { useState } from "react";
const items = [{ q: "Is MedLink an online pharmacy?", a: "No. MedLink coordinates requests with licensed local pharmacies holding verified stock; it does not dispense medicine itself." }, { q: "Can AI approve or substitute medicine?", a: "No. Automation can help read and match requests, but a licensed pharmacist remains responsible for clinical review and eligible substitutions." }, { q: "How is patient information protected?", a: "Access is role-based, tenant-isolated, encrypted, and auditable. Information is limited to the people handling the request." }, { q: "What happens when nearby stock is unavailable?", a: "The search can expand to nearby areas and a pharmacist can review eligible alternatives. Nothing is changed automatically." }];
export function FAQ() { const [open, setOpen] = useState(0); return <section id="faq" className="section section--light"><div className="container faq"><header><p className="eyebrow">Questions, answered</p><h2>What people ask before getting started</h2></header><div>{items.map((item, index) => { const expanded = open === index; return <article key={item.q}><h3><button aria-expanded={expanded} aria-controls={`faq-${index}`} onClick={() => setOpen(expanded ? -1 : index)}>{item.q}<span>{expanded ? "−" : "+"}</span></button></h3>{expanded && <p id={`faq-${index}`}>{item.a}</p>}</article>; })}</div></div></section>; }
