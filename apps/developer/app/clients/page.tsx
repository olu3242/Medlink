import{ClientManager}from"../../components/client-manager";import{clients,type Client}from"../../lib/api";export default async function Page(){let rows:Client[]=[];let failed=false;try{rows=await clients()}catch{failed=true}return <><header className="head"><div><div className="eyebrow">Credentials</div><h1>API clients</h1><p className="muted">Issue least-privilege clients and revoke access when no longer required.</p></div></header>{failed?<p className="error" role="alert">Existing clients could not be loaded.</p>:null}<ClientManager initial={rows}/></>}
