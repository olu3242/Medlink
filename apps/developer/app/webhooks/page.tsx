import{WebhookManager}from"../../components/webhook-manager";import{deliveries,webhooks,type Delivery,type Webhook}from"../../lib/api";
export default async function Page(){let hooks:Webhook[]=[];let logs:Delivery[]=[];let failed=false;try{[hooks,logs]=await Promise.all([webhooks(),deliveries()])}catch{failed=true}return <><header className="head"><div><div className="eyebrow">Event delivery</div><h1>Webhooks</h1><p className="muted">Configure endpoints and inspect delivery attempts.</p></div></header>{failed?<p className="error" role="alert">Some webhook information is unavailable.</p>:null}<WebhookManager initial={hooks}/><section className="card table" style={{marginTop:"1rem"}}><h2>Recent deliveries</h2><table><thead><tr><th>Event</th><th>Status</th><th>HTTP</th><th>Attempts</th><th>Time</th></tr></thead><tbody>{logs.map(x=><tr key={x.id}><td>{x.event}</td><td><span className={`badge ${x.status==="failed"?"fail":x.status==="pending"?"warn":""}`}>{x.status}</span></td><td>{x.statusCode??"—"}</td><td>{x.attempts}</td><td>{new Date(x.createdAt).toLocaleString()}</td></tr>)}</tbody></table></section></>}
