export default function Page(){const example=`curl https://api.medlink.example/api/v1/medicines?q=amoxicillin \\\n  -H "Authorization: Bearer $MEDLINK_API_TOKEN" \\\n  -H "Accept: application/json"`;return <><header className="head"><div><div className="eyebrow">API documentation</div><h1>Quickstart</h1><p className="muted">Make your first tenant-scoped MedLink API request.</p></div></header><section className="card"><h2>1. Create a least-privilege client</h2><p>Create an API client with only the scopes your integration needs. Store credentials in a secrets manager; never commit them or expose them in browser code.</p><h2>2. Set the token securely</h2><div className="code" role="region" aria-label="Environment variable example"><code>MEDLINK_API_TOKEN=&lt;read from your secrets manager&gt;</code></div><h2>3. Call a versioned endpoint</h2><pre className="code"><code>{example}</code></pre><h2>4. Handle reliability</h2><p>Use idempotency keys for writes, honor rate-limit headers, verify webhook signatures, and retry transient failures with exponential backoff.</p><div className="notice">Never log authorization headers, patient payloads, webhook signatures, or raw API credentials.</div></section></>}
