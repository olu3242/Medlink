# Engine Status Matrix

Status reflects executable source and evidence, not directory names.

| Engine | Wave | Status | Certification | Blocking issues | Next action |
| --- | --- | --- | --- | --- | --- |
| Identity and Tenant Context | 1 | Partial | Conditional | Canonical context now used by `apps/admin`, `apps/patient`, and `apps/web`; no live-RLS evidence | Execute and test migrations, then freeze |
| RBAC | 1 | Partial | Conditional | Role map enforced in every route's API pipeline call; no live authorization-denial evidence | Add live RLS/RBAC denial matrix |
| Database Platform | 1 | Partial | Conditional | Strong static migration; no runtime/RLS evidence | Execute and test migrations |
| Observability | 1 | Scaffolded | Conditional | Logger, correlation, and real (non-hardcoded) dependency-aware health checks exist; no metrics/tracing dashboards or SLOs. `apps/web`'s and `packages/api`'s independently-duplicated audit/events/telemetry wiring (which had drifted - the web copy was missing an `event` attribute the api copy already tagged) is now one shared `standardRuntimeHooks()` in `packages/observability` | Add propagation, metrics, traces, alerting |
| Medicine Knowledge | 2 | Partial | Conditional | Domain/catalog schema exists; create/update commit business state and evidence atomically (migration 008); the admin catalog UI/API response shape was fixed (it was returning raw snake_case rows against camelCase client types, so the table rendered blank for every column but id/status - see `apps/admin/lib/application.test.ts`); `findGenericById`/`findBrandsByIngredientIds` now query real tables (migration 202607290011 added `generics`); admin catalog's own `brands()`/`generics()`/`list()`/`get()` still bypass `packages/medicine`'s repository by design (closed-vocabulary 404 risk, see `docs/wave-2-certification.md`) | Load-test; add integration/RLS tests |
| Medication Equivalency | 2 | Partial | Conditional | Safety unit tests pass; tenant review workflow now wired end-to-end (`PATCH /api/v1/equivalents/{id}/review`, migration 009, `CatalogEquivalencyService.assertReviewed`); no integration/RLS tests | Add integration/RLS tests |
| Prescription Intelligence | 2 | Partial | Conditional | Parser now wired end-to-end (`POST /api/v1/prescriptions/{id}/extract`, migration 009) against a placeholder reader; also wrapped as WF-004's real executable workflow step (`packages/workflows/src/prescription-parsing.ts`); OCR adapter still unselected | Select/configure OCR provider |
| Clinical Intelligence | 2 | Partial | Conditional | Validation rules now wired end-to-end (`POST /api/v1/prescriptions/{id}/validate`, migration 009); three advisory-only rules (`DuplicateTherapyRule`, `PatientAllergyRule`, `PolypharmacyRiskRule`) | Add integration/RLS tests |
| Search | 2 | Partial | Conditional | `GET /api/v1/search` now wired against real trigram index adapters for both brand and generic medicines (migration 202607290011 added the generic index); no production-scale index evidence | Load-test the trigram index |
| Conversation Engine | 3 | Partial | Conditional | `packages/conversation` (models/ports/`ConversationEngine`/default keyword intent classifier), migration `202607290012` (`conversations`/`conversation_messages`/append-only `conversation_events`, RLS), and Supabase-backed port implementations (`apps/web/lib/conversation-store.ts`) all exist with unit and static RLS/migration tests; no route yet, blocked on the `RuntimeContext.userId` ADR below (`docs/adr/0004-conversation-runtime-webhook-identity.md`, drafted not accepted) | Accept ADR 0004, then wire a route |
| Workflow Orchestrator | 3 | Partial | Conditional | Generic step runner carries context between steps (`packages/workflows/src/service.ts`); all 15 canonical workflows have a structural step-name definition (`definitions.ts`); WF-003, WF-004, WF-005, WF-006, WF-007 (two steps), WF-008, and WF-009 each have at least one real executable step (eight steps across seven workflows), each one guarded against drifting from its structural definition by a dedicated consistency test. Durable via `workflow_instances` (migration 202607290015) and `SupabaseWorkflowStore`; `WorkflowOrchestratorInvoker` wires `packages/conversation`'s `WorkflowInvoker` port to it (though only for `medicine_search`, the one intent `KeywordIntentClassifier` currently produces that has a wired step). No route calls any of this yet -- blocked on ADR 0004, same as Batch 3.1; no recovery model; the other 8 workflows remain structural only | Accept ADR 0004, then wire a route |
| WhatsApp Adapter | 3 | Partial | Conditional | `packages/whatsapp` (signature verification, Cloud API payload normalization, `GraphApiWhatsAppSender`) and `conversation_channel_bindings` (migration `202607290013`) exist with unit and static RLS/migration tests; no webhook route — blocked on `packages/runtime`'s `runtimeContextSchema` requiring a non-optional `userId`, which an unauthenticated provider webhook never has (see `docs/audit/RC1_BACKLOG.md` item 15) | Accept an ADR deciding how a webhook/system identity is represented in `RuntimeContext` |
| Medication Access Request | 3 | Partial | **Conditional (partial reachability)** | Strong domain/database rules; creation is now atomic (migration 202607290016's `create_mar`); clinical review decision is atomic and idempotent-replay-safe (migration 202607290017/019's `decide_clinical_review`). **`created → validated → reviewed` is now a real, reachable chain** (migration 202607290018's `validate_mar`, migration 202607290019 advancing the MAR to `reviewed` on approval). **`searching`/`matched` remain unimplemented** — `reserve_inventory` requires `state = 'matched'`, so no MAR can yet reach a reservable state, and WF-008's inventory search isn't MAR-scoped today. patient home/detail UI response shape fixed | Decide how `searching`/`matched` relate to WF-008 (see RC1_BACKLOG item 19), then implement |
| Pharmacy Discovery | 3 | Partial | Conditional | Domain service and schema; patient search UI response shape fixed (was rendering blank and crashing on a fabricated `distanceKm` field with no backing calculation; `/api/v1/inventory`'s `q` filter was silently ignored, now applied); also wrapped as WF-008's real executable workflow step (`packages/workflows/src/inventory-discovery.ts`) | Wave 3 integration |
| Inventory | 3 | Partial | Conditional | Locking model exists; no runtime concurrency tests | Wave 3 integration |
| Reservation | 3 | Partial | Conditional | `reserve_inventory` is now implemented (migration 010) - it was referenced by `AccessApplication.reserve()` but undefined, so this path 500'd unconditionally; orchestrates the existing atomic lock trigger rather than new concurrency logic. Its idempotent replay path now validates the replay request matches the original reservation's MAR/pharmacy/batch/quantity before trusting the key (migration 202607290020 - found by PR review; a reused key with different resources previously returned the unrelated earlier reservation as a false success). Still open: `apps/patient/app/reserve/[inventoryId]/page.tsx` posts `{inventoryId}` only, missing `marId`/`pharmacyLocationId`/`quantity`/`expiresAt`, and nothing transitions a MAR to `matched` yet - the UI cannot successfully call this function until Wave 3's workflow orchestration exists. State vocabulary still differs across layers (`packages/reservations`: active/expired/cancelled/fulfilled vs. DB `reservation_status`: pending/confirmed/ready/collected/cancelled/expired) - confirmed low-risk on audit: `packages/reservations` has zero real callers anywhere in the repository (only self-referenced in its own `package.json`), so this is dead-code drift, not a live bug; every actual consumer (`apps/patient/lib/application.ts`'s `toMar`) passes the real DB enum through honestly | Wave 3: MAR state progression + reservation UI |
| Notification | 3/5 | Partial | Conditional | Service/outbox schema; TS service omits WhatsApp channel | Add channel-neutral contracts in Wave 3 |
| Pharmacy Portal | 4 | Scaffolded | Fail | `apps/pharmacy/app/reservations/page.tsx` has never worked: it bypasses its own `lib/api.ts` cross-origin client for a same-origin relative fetch that 404s locally, PATCHes a status value (`"declined"`) the `reservation_status` DB enum has never had, and no `GET`/`PATCH /api/v1/reservations[/{id}]` route exists anywhere in the repo regardless; no auth/RBAC evidence | Wave 4 |
| Pharmacist Portal | 4 | Scaffolded | Fail | Decision form previously posted to a nonexistent `/api/v1/review/{id}/decision` endpoint with mismatched enum values and field names; now correctly calls `PATCH /api/v1/review/{id}` with the real contract. Still fails certification: no session of its own to authenticate the cross-origin call, no RBAC evidence | Wave 4: portal authentication |
| Hospital Portal | 4 | Scaffolded | Fail | Provider UI calls missing APIs | Wave 4 |
| Administrator Portal | 4 | Partial | Conditional | Catalog UI/API exists; API pipeline now conformant repository-wide | Wave 4 |
| Payment | 5 | Partial | Conditional | Token boundary exists; no provider adapter/runtime tests | Wave 5 |
| Adherence | 5 | Scaffolded | Fail | Minimal service and UI; no canonical workflow | Wave 5 |
| AI Coordinator | 5 | Partial | Conditional | Safety wrapper exists; agent catalog incomplete | Align agent catalog |
| Analytics and Reporting | 5 | Scaffolded | Fail | Privacy suppression primitives only | Wave 5 |
| Governance and Consent | 5 | Partial | Conditional | Schema/service primitives; no end-to-end audit integration | Wave 5 |
| Partner Integrations | 5 | Scaffolded | Fail | Typed ports and webhook guard only | Wave 5 |
| Security | 5 | Scaffolded | Fail | Policy primitive and schema; no operational controls evidence | Wave 5 |
| Certification | 5 | Scaffolded | Fail | Generic runner only; no registered certification suite | Build evidence-driven suite |
| Developer/Operations Portal | 5 | Scaffolded | Fail | UI calls missing enterprise APIs | Wave 5 |

No engine is fully certified. Wave 1 is functionally established but remains
conditionally certified until route conformance and runtime infrastructure
evidence are complete.
